# Relationships

Inspect relation endpoint constraints before using `relate` or `unrelate`. Supply
declared edge properties with repeated `--property name=value` flags. Ordinary
Markdown links remain informational and are not substitutes for managed relations.
