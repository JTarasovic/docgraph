- name: Install pinned companion runtime
  uses: ./install-runtime
  id: runtime

- name: Stage pinned companion runtime and portable files
  run: cargo xtask release stage --runtime "${{ steps.runtime.outputs.executable }}"
