- name: Install pinned companion runtime
  uses: ./install-runtime
  id: runtime

- name: Stage pinned companion runtime and portable files
  shell: bash
  env:
    RELEASE_RUNTIME: ${{ steps.runtime.outputs.executable }}
  run: bash tools/release/stage.sh "$RELEASE_RUNTIME"
